<%= form_with(model: [@equip, @equipslot], local: true) do |form| %>
  <% if @equipslot.errors.any? %>
     <div class="alignboxes">
        <div id="errorexplan">
           <h2 id="errorheader"><%= pluralize(@equipslot.errors.count, "error") %> prohibited this equipslot from being saved:</h2>
           <ul>
              <% @equipslot.errors.full_messages.each do |message| %>
                 <li class="explantext"><%= message %></li>
              <% end %>
           </ul>
        </div>
     </div>
  <% end %>
  <div class="pageformat">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>
  <br>
  <div class="pageformat">
    <%= form.submit class: "button submitbutton" %>
  </div>
  <br>
  <p class="formlinks"><%= link_to 'Back', partner_equip_path(@equip.partner, @equip) %></p>
<% end %>
