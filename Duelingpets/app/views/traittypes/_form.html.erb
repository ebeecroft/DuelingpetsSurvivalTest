<%= form_with(model: traittype, local: true) do |form| %>
  <% if traittype.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(traittype.errors.count, "error") %> prohibited this traittype from being saved:</h2>

      <ul>
      <% traittype.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
