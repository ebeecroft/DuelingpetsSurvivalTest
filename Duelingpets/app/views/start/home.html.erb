<% provide(:title, "Duelingpets: Homepage") %>
<% provide(:description, "Welcome to Duelingpets Home of the Imaginative Creatures. We have many new and exciting content that might be worth your while. All the content here is user generated by many talented artists, writers, composers, and channel owners. Feel free to give this place a browse to see what is new.") %>
<% provide(:keywords, "virtualpet, digitalpets") %>
<% provide(:robots, "index, follow") %>
<% provide(:h1, "Welcome to Duelingpets #{homepageAlerts}") %>
<h2 class="subheader1">Traveling Companions</h2>
<div class="imagesize">
<% newestContent("OC").each_with_index do |content, index| %>
   <p class="largeimagecontainer"><%= image_tag(url_for(content.thumbnail), class: "fitlargeimages") %></p>
<% end %>
</div>
<br>
<h2 class="subheader1">Artworks</h2>
<div class="imagesize">
<% newestContent("Art").each_with_index do |content, index| %>
   <p class="largeimagecontainer"><%= image_tag(url_for(content.image), class: "fitlargeimages") %></p>
<% end %>
</div>
<br>
<h2 class="subheader1">Movies</h2>
<div class="imagesize">
<% newestContent("Movie").each_with_index do |content, index| %>
   <p class="videocontainer"><%= video_tag(url_for(content.clip), poster: "#{url_for(content.thumbnail)}", class: "fitvideo", :controls => true) %></p>
<% end %>
</div>
<br>
<h2 class="subheader1">Creatures</h2>
<div class="imagesize">
<% newestContent("Creature").each_with_index do |content, index| %>
   <p class="largeimagecontainer"><%= image_tag(url_for(content.image), class: "fitlargeimages") %></p>
<% end %>
</div>
<br>
<h2 class="subheader1">Monsters</h2>
<div class="imagesize">
<% newestContent("Monster").each_with_index do |content, index| %>
   <p class="largeimagecontainer"><%= image_tag(url_for(content.image), class: "fitlargeimages") %></p>
<% end %>
</div>
<br>
<h2 class="subheader1">Tunes</h2>
<div class="imagesize">
<% newestContent("Sound").each_with_index do |content, index| %>
   <p class="imagecontainer"><%= audio_tag(url_for(content.song), controls: '', class: "fitaudio") %></p>
<% end %>
</div>
<br>
<h2 class="subheader1">Items</h2>
<div class="imagesize">
<% newestContent("Item").each_with_index do |content, index| %>
   <p class="smallimagecontainer"><%= image_tag(url_for(content.image), class: "fitsmallimages") %></p>
<% end %>
</div>
<br>
<h2 class="subheader1">Elements</h2>
<div class="imagesize">
<% newestContent("Element").each_with_index do |content, index| %>
   <p class="smallimagecontainer"><%= image_tag(url_for(content.image), class: "fitsmallimages") %></p>
<% end %>
</div>
<br>
<h2 class="subheader1">Books</h2>
<div class="imagesize">
<% newestContent("Book").each_with_index do |content, index| %>
   <p class="smallimagecontainer"><%= image_tag(url_for(content.bookcover), class: "fitsmallimages") %></p>
<% end %>
</div>
