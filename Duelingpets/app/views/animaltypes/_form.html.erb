<%= form_with(model: animaltype, local: true) do |form| %>
  <% if animaltype.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(animaltype.errors.count, "error") %> prohibited this animaltype from being saved:</h2>

      <ul>
      <% animaltype.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
