<%= form_with(model: damageoffset, local: true) do |form| %>
  <% if damageoffset.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(damageoffset.errors.count, "error") %> prohibited this damageoffset from being saved:</h2>

      <ul>
      <% damageoffset.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :value %>
    <%= form.text_field :value %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
