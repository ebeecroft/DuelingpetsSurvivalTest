<%= form_with(model: characterstat, local: true) do |form| %>
  <% if characterstat.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(characterstat.errors.count, "error") %> prohibited this characterstat from being saved:</h2>

      <ul>
      <% characterstat.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :value %>
    <%= form.number_field :value %>
  </div>

  <div class="field">
    <%= form.label :character_id %>
    <%= form.number_field :character_id %>
  </div>

  <div class="field">
    <%= form.label :stat_id %>
    <%= form.number_field :stat_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
